name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PYTEST_DISABLE_PLUGIN_AUTOLOAD: "1"
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install package
        run: python -m pip install -e ".[dev]"

      - name: Validate dependencies
        run: python -m pip check

      - name: Run tests
        env:
          RETRIEVAL_BACKEND: local
        run: python -m pytest -q

      - name: Enforce answer quality gates
        run: >-
          python -m multi_agent_rag eval
          --orchestrator local
          --retrieval-backend local
          --min-pass-rate 1.0
          --min-average-grounding 0.8

      - name: Enforce retrieval quality gates
        run: >-
          python -m multi_agent_rag retrieval-eval
          --retrieval-backend local
          --top-k 5
          --min-pass-rate 1.0
          --min-average-recall 1.0
          --min-mrr 0.6
          --min-ndcg 0.7

  frontend:
    name: Frontend checks
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: frontend
    env:
      NEXT_TELEMETRY_DISABLED: "1"
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Check types
        run: npm run typecheck

      - name: Build application
        run: npm run build

  containers:
    name: Container configuration
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Validate Compose configuration
        run: docker compose config --quiet

      - name: Check backend Dockerfile
        run: docker buildx build --check --build-arg INSTALL_PRODUCTION_EXTRAS=false .

      - name: Check frontend Dockerfile
        run: docker buildx build --check frontend
